import React from 'react'

const Footer = () => {
  return (
    <footer className={` container-fluid mt-5 pt-5`}>
      <div className={` bg-dark  text-center `}>
        <p className={` lead  p-5  text-uppercase fw-semibold `}>Roni Ribeiro</p>
      </div>
    </footer>
  )
}

export default Footer